language: dart
sudo: required
dist: trusty
dart:
  - stable
  - 2.7.0
  - dev
before_script:
  - source $(pub run chrome_travis:env_rc)
script:
  - dart tool/travis.dart
matrix:
  allow_failures:
    - dart: dev